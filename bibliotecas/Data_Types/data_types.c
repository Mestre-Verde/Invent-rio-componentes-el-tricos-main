#include "data_types"

#include <stdio.h>
        

void printSizeof() {
    printf("format\t\t\tnº of bytes\t2^n")
  for (i = 0; i < maxValue; i++) {
    printf("%c\t\t\t%zu\t%i",datatypes[i],sizeof(vartype[i]),)
  }
}